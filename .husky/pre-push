# Skip LFS push verification when git-lfs isn't installed instead of failing the
# push. Only the two .gitattributes-tracked LFS files are affected, and anyone
# without git-lfs couldn't upload their objects anyway.
command -v git-lfs >/dev/null 2>&1 || exit 0
git lfs pre-push "$@"
