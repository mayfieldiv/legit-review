diff --git a/docs/old-notes.md b/docs/old-notes.md
deleted file mode 100644
index 38d4da1..0000000
--- a/docs/old-notes.md
+++ /dev/null
@@ -1,3 +0,0 @@
-# Old notes
-
-These notes are stale and unloved.
diff --git a/src/utils.ts b/src/helpers.ts
similarity index 50%
rename from src/utils.ts
rename to src/helpers.ts
index 19b5084..54a90eb 100644
--- a/src/utils.ts
+++ b/src/helpers.ts
@@ -1,7 +1,8 @@
 export interface Config {
+  env: string;
   port: number;
 }
 
 export function loadConfig(): Config {
-  return { port: 8080 };
+  return { env: process.env.NODE_ENV ?? 'development', port: 8080 };
 }
diff --git a/src/logger.ts b/src/logger.ts
new file mode 100644
index 0000000..640c50c
--- /dev/null
+++ b/src/logger.ts
@@ -0,0 +1,3 @@
+export function logInfo(message: string): void {
+  console.log(`[info] ${message}`);
+}
diff --git a/src/server.ts b/src/server.ts
index 07d654e..665cef5 100644
--- a/src/server.ts
+++ b/src/server.ts
@@ -1,14 +1,14 @@
 import { createServer } from 'node:http';
 
-import { loadConfig } from './utils';
+import { loadConfig } from './helpers';
 
 const config = loadConfig();
 
 export function startServer(): void {
   const server = createServer((request, response) => {
     if (request.url === '/health') {
-      response.writeHead(200, { 'Content-Type': 'text/plain' });
-      response.end('ok');
+      response.writeHead(200, { 'Content-Type': 'application/json' });
+      response.end(JSON.stringify({ status: 'ok' }));
       return;
     }
 
@@ -17,11 +17,11 @@ export function startServer(): void {
   });
 
   server.listen(config.port, () => {
-    console.log(`listening on ${config.port}`);
+    console.log(`listening on ${config.port} (${config.env})`);
   });
 }
 
 export function stopServer(): void {
-  console.log('stopping');
-  process.exit(0);
+  console.log('stopping gracefully');
+  process.exitCode = 0;
 }
